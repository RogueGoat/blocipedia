 <div class="row">
   <div class="col-md-4">
     <p>Need some tips before creating your Chomp?</p>
     <ul>
       <li>Make it relevant!</li>
       <li>Remember to include sources for your information.</li>
       <li>Add pictures, quotes, and personal anecdotes to make your topic super interesting!</li>
     </ul>
   </div>
   <div class="col-md-6">
   <%= form_for @wiki do |wiki| %>

    <div class="form-group">
      <%= wiki.label :title %>
      <%= wiki.text_field :title, class: 'form-control', placeholder: 'Enter CHOMP Title' %>
    </div>
    <div class="form-group">
      <%= wiki.label :body %>
      <%= wiki.text_area :body, class: 'form-control', placeholder: 'Enter your FINtformation' %>
    </div>
      <% if current_user.admin? || current_user.premium? %>
      <div class="form-group">
        <%= wiki.label :private, class: 'checkbox' do %>
          <%= wiki.check_box :private %> Private topic
        <% end %>
      </div>
    <%= wiki.submit "Save", class: 'btn btn-success' %>
    <% end %>
    <% end %>
   </div>
 </div>